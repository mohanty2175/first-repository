CREATE TABLE "SCOTT"."TICKETBUS" 
   (	"TICKETID" NUMBER NOT NULL ENABLE, 
	"PASSANGERNAME" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"PASSANGERFROM" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"PASSANGERTO" VARCHAR2(20 BYTE) NOT NULL ENABLE, 
	"PASSANGERAGE" NUMBER NOT NULL ENABLE, 
	"TICKETPRICE" FLOAT(126) NOT NULL ENABLE
   );